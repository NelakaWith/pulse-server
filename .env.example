# Environment Configuration Example
# Copy this file to .env.local and .env.production, then fill in your values

# Server Configuration
NODE_ENV=development
PORT=3000

# API Key Authentication
# Enable API key validation for all /api routes
API_KEY_AUTH_ENABLED=true

# API Keys - Comma-separated list of valid keys
# Development keys format: sk-dev-[random-hex]
# Production keys format: sk-prod-[random-hex]
# Generate new keys with: node utils/generateApiKey.js [environment] [count]
API_KEYS=sk-dev-abc123,sk-dev-def456,sk-prod-ghi789

# OpenRouter AI Configuration
# Get your API key from https://openrouter.ai/keys
OPENROUTER_API_KEY=sk-or-v1-xxxxx
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1

# Default AI Model
# Free models: google/gemma-3-27b-it:free, deepseek/deepseek-chat-v3.1:free
DEFAULT_AI_MODEL=deepseek/deepseek-chat-v3.1:free

# Optional AI Request Parameters
# MAX_TOKENS=1000
# TEMPERATURE=0.7

# GitHub Configuration
# Generate personal access token at https://github.com/settings/tokens
GITHUB_TOKEN=github_pat_xxxxx
GITHUB_REST_API_BASE_URL=https://api.github.com
GITHUB_GRAPHQL_API_BASE_URL=https://api.github.com/graphql

# CORS Configuration
# For development: "*" (allow all)
# For production: comma-separated list of allowed origins
CORS_ORIGIN=http://localhost:3001,http://localhost:3002

# JWT Configuration (Optional - for future implementation)
# JWT_SECRET=your-secret-key-min-32-chars-long
# JWT_EXPIRY=24h
# REFRESH_TOKEN_EXPIRY=7d

# Rate Limiting (Optional - defaults are in config/index.js)
# RATE_LIMIT_WINDOW_MS=900000
# RATE_LIMIT_MAX_PER_IP=100
# RATE_LIMIT_MAX_PER_KEY=500

# Logging (Optional)
# LOG_LEVEL=info|debug|warn|error
# LOG_LEVEL=info